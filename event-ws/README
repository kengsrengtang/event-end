NOTES for various things that we are doing:

Freebies that we are using:
- node.js
- node mysql
- frisby
- express

To run the server:
- cd event-ws
- npm install
- npm start


To run on ubuntu:
- export NODE_ENV=production
- npm start

To use mysql on Ubuntu:
- mysql --user=root -password=mkh123
